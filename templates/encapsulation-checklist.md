# Encapsulation Checklist

- Are fields hidden unless exposure is necessary?
- Are invariants protected against partial or invalid mutation?
- Is mutable internal state leaked directly?
- Are setters too broad?
- Is API exposure larger than needed?
